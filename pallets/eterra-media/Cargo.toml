[package]
name = "pallet-eterra-media"
version = "0.1.0"
edition = "2021"
license = "Unlicense"
authors = ["Eterra <dev@eterra.example>"]
description = "Generic media registry pallet for Eterra and other  games (images, audio, skins, etc.)"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu"]

[dependencies]
codec = { workspace = true, default-features = false, features = ["derive"] }
scale-info = { workspace = true, default-features = false, features = ["derive"] }

sp-core = { workspace = true, default-features = false }
sp-std  = { workspace = true, default-features = false }
sp-runtime = { workspace = true, default-features = false }

frame-support = { workspace = true, default-features = false, features = ["experimental"] }
frame-system  = { workspace = true, default-features = false }

[dev-dependencies]
sp-io = { workspace = true, default-features = false }

[features]
default = ["std"]
std = [
    "codec/std",
    "scale-info/std",
    "sp-core/std",
    "sp-std/std",
    "sp-runtime/std",
    "frame-support/std",
    "frame-system/std",
]
